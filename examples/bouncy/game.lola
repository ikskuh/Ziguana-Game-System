const ball = LoadData("ball.ico");

Print("Hello, World!");

Sleep(1);

GpuSetBorder(0);
SetGraphicsMode(true);

var x = 56;
var y = 41;

while(true)
{
  x += JoyGetX();
  y += JoyGetY();

  GpuFillRect(0, 0, 120, 90, 0);

  GpuBlitBuffer(x, y, 8, ball);

  GpuFlush();
}